import React from "react"

export const UserProfile = () => {
    return (
        /*  if we don't want to wrapper div then we use React.fragment 
         if we want shortcut then we have to used <> </> 
         In JSX every tag must be closed */


        <React.Fragment>
            <h1>Hello </h1>
            <p>Author</p>
        </React.Fragment>



    )
}