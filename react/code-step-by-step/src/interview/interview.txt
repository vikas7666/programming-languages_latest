1) What is diffrence between library and framework.

library : library ke flow hum log control karate hai. e.g React
framework : has its own flow control   e.g Angular
jsx : Javascript xml 
tsx : typeScript xml


package.json
Most important file without pacakage json we cannot run our project

2) diff dependencies && devDependencies 

devDependencies
dependent package we are rely on doing developemnt 

dependencies
jiske upar apana project run ho raha hai

3)  diff component && function

component : Always start with Capital letter, can return HTML+ JSX code
function : Always start with small letter

e.g Component

function Start(){
    return (
        <h1>Hello</h1>
    )
}

e.g function 
function will not return html code and they are used for logical operation

4) How many way we can export component?
Default component
Name export
multiple export

export deafault
export default means in one file contain maximum component then its knows as multiple export

5) How to check React Version 

package.json
 "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },

6) What is mean by 19.1.1
  means 19 major release , 1 means minior release , 1 means patch

7) State
   state is container to store data like varibale
   This is mutable and dyanmic
   We have to import it when we want to use it.
   It re-render component so that data can visible on UI.

8) UseEffect 
  can we use multiple useEffect in a page?
  yes we can use as many as want